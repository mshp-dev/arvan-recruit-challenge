{{- if .Values.secrets.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "geo_loc_api.fullname" . }}-secrets
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $val := .Values.secrets.values }}
  "{{ $key }}": "{{ $val }}"
  {{- end }}
{{- end }}