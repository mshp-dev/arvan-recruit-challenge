apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "geolocapi.fullname" . }}-envs
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $val := .Values.envs }}
  "{{ $key }}": "{{ $val }}"
  {{- end }}
